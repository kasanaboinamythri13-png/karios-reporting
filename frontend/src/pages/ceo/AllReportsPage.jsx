// Owner: Member 3
// TODO: GET /api/reports with filters (department, status, date range) → table → click opens /reports/:id
export default function AllReportsPage() {
  return (
    <>
      <h1>All reports</h1>
      <div className="card muted">Filters + reports table go here.</div>
    </>
  );
}
