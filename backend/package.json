{
  "name": "karios-reporting-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Karios Reporting API (Node.js + Express)",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^18.0.3",
    "express": "^5.2.1",
    "helmet": "^8.3.0"
  },
  "devDependencies": {
    "supertest": "^7.3.0"
  }
}
